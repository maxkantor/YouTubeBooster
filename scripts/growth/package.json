{
  "name": "youtubebooster-growth-scripts",
  "private": true,
  "type": "module",
  "description": "Local/CLI growth measurement helpers (GA4 + Stripe).",
  "scripts": {
    "test": "node --test tests/**/*.test.mjs"
  },
  "dependencies": {
    "google-auth-library": "^10.9.1"
  }
}
