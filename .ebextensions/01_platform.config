option_settings:
  aws:elasticbeanstalk:container:python:
    WSGIPath: web_app.py
  aws:elasticbeanstalk:application:environment:
    FLASK_ENV: production
    AUTO_OPEN: "0"
    # Set GOOGLE_CREDENTIALS_BASE64 via `eb setenv` (do not commit secrets)
