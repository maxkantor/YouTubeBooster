# Root `artifacts/` folder

**Lambda zip is not here.** The packaged API is only:

- **`backend/youtubebooster-api.zip`** (generated by `./backend/publish-lambda.sh`; gitignored).

See **`backend/LAMBDA-DEPLOY.md`**.
